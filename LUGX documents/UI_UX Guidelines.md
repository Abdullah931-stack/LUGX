قمت بصياغة هذا الدليل ليكون المرجع البصري والتقني الموحد لمشروع **LUGX**.

تم تصميم هذا الدليل ليحقق التوازن الصعب بين "الوظيفة الهندسية المعقدة" و"المظهر الجمالي البسيط"، معتمداً على مكتبات `shadcn/ui` و `Tailwind CSS` كأساس تقني.

---

# 📐 دليل تصميم واجهة وتجربة المستخدم (LUGX UI/UX Guidelines v1.0)

**التصنيف:** سري ومقيد لفريق التطوير والتصميم.
**الفلسفة:** "الفخامة الهندسية" (Engineering Luxury).

---

## 1. الرؤية البصرية (Visual Direction)

### 1.1 المفهوم: الفخامة الصامتة (Quiet Luxury)

نتخلى عن الضجيج البصري. الفخامة في LUGX لا تعني الزخرفة، بل تعني **"الدقة"** و **"الخامة"**. يجب أن تشعر واجهة المستخدم وكأنها مصنوعة من مواد عالية الجودة (تيتانيوم مصقول، زجاج معتم، ألياف كربون) وليست مجرد بكسلات ملونة.

* **المبدأ:** "المحتوى هو البطل". الواجهة تتراجع للخلف لتفسح المجال للنص وأدوات الذكاء الاصطناعي.
* **الإحساس:** صلب، سريع الاستجابة، غامض، ومريح للعين في جلسات العمل الطويلة.

### 1.2 استخدام الفراغ (Negative Space Strategy)

المساحة البيضاء (أو السوداء في حالتنا) ليست فراغاً، بل هي عنصر تصميم نشط.

* **قاعدة التنفس:** لا تزدحم العناصر. اترك هوامش (Padding/Margins) سخية حول كتلة المحرر الرئيسية.
* **التركيز:** استخدام الفراغ لتوجيه عين المستخدم نحو زر "الإجراء الأساسي" (مثل توليد الذكاء الاصطناعي) دون الحاجة لأسهم أو ألوان صارخة.

---

## 2. نظام التصميم (Design System)

### 2.1 لوحة الألوان (Color Palette) - Dark Mode Only

نعتمد نظام الألوان الأحادي (Monochromatic) القائم على تدرجات الـ **Zinc** لإنشاء عمق بصري دون تشتيت.

* **الخلفيات (Surfaces):**
* **الأساس (Void):** `bg-zinc-950` (الخلفية الرئيسية، أسود مطفأ وليس أسود كامل #000).
* **الطبقة الأولى (Elevated):** `bg-zinc-900/50` (للشريط الجانبي والكروت).
* **الحدود (Borders):** `border-zinc-800` (حدود فائقة الرقة).


* **النصوص (Typography Colors):**
* **العناوين:** `text-zinc-50` (أبيض نقي تقريباً).
* **النص الأساسي:** `text-zinc-300` (رمادي فاتح لراحة العين).
* **النص الثانوي:** `text-zinc-500` (للعناصر غير النشطة).


* **ألوان التمييز (Accents) - "نبض الذكاء الاصطناعي":**
* يستخدم لون واحد فقط للإشارة للذكاء الاصطناعي أو الإجراءات الرئيسية.
* **اللون:** `Electric Indigo` أو `Violet` مع تأثير توهج خافت (Glow).
* **الكود:** `indigo-500` (يستخدم بنسبة شفافية منخفضة في الخلفيات، وقوية في النصوص).



### 2.2 الطبوغرافيا (Typography)

نستخدم خطوطاً هندسية (Geometric Sans) تعكس الحداثة وتدعم العربية والإنجليزية بانسجام تام.

* **الخط الإنجليزي:** `Geist Sans` أو `Inter` (مع تباعد أحرف `tracking-tight` للعناوين).
* **الخط العربي:** `IBM Plex Sans Arabic` أو `Readex Pro` (خطوط هندسية منخفضة التباين تناسب الواجهات التقنية).
* **الأوزان:**
* الاعتماد على الوزن الخفيف (`Light` / `Regular`) للنصوص لتعزيز الأناقة.
* استخدام الوزن المتوسط (`Medium`) للعناوين. يُمنع استخدام `Bold` الثقيل إلا في الضرورة القصوى.



### 2.3 الأيقونات (Iconography)

* **المكتبة:** `Lucide React` (المعيار مع shadcn).
* **النمط:** خطي (Stroke)، بسمك رفيع وموحد (1.5px).
* **الحجم:** صغير ودقيق (16px - 20px). الأيقونات الضخمة ممنوعة.

---

## 3. المكونات التفاعلية (UI Components)

### 3.1 الأزرار (Buttons)

* **الشكل:** مستطيلات بحواف ناعمة قليلاً (`rounded-md` أو ما يعادل 6px). تجنب الحواف الدائرية تماماً (`rounded-full`) إلا للأزرار العائمة الصغيرة.
* **الحالة العادية:** خلفية داكنة (`bg-zinc-900`) مع حدود فاتحة جداً (`border-zinc-800`) وتوهج داخلي طفيف عند التحويم (Hover).
* **أزرار الـ AI:** زر التوليد يجب أن يتميز بتدرج لوني خافت جداً أو حدود مضيئة (`border-indigo-500/50`).

### 3.2 حقول الإدخال (Inputs & Editor)

* **المحرر (TipTap):** خالٍ من الحدود تماماً. النص يطفو في الفراغ. مؤشر الكتابة (Caret) يجب أن يكون بلون الـ Accent وبحركة ناعمة.
* **الحقول النمطية:** خلفية شفافة أو شبه شفافة، مع خط سفلي فقط أو حدود رمادية داكنة جداً تختفي عند عدم التركيز.

### 3.3 الحركات الانتقالية (Micro-interactions)

الفخامة تعني "الهدوء". الحركات يجب أن تكون غير محسوسة.

* **السرعة:** بطيئة نسبياً (300ms - 500ms).
* **النوع:** `ease-out`. تجنب الحركات "النطاطة" (Bouncy/Spring) لأنها توحي بالمرح واللعب، ونحن نستهدف الاحترافية.
* **الظهور:** العناصر تظهر بتلاشي ناعم (Fade-in) مع حركة بسيطة للأعلى.

---

## 4. المعايير والقيود (Standards & Constraints)

بناءً على وثيقة المتطلبات (PRD) والمعطيات التقنية:

1. **قيد التباين (Contrast Constraint):**
* يجب الحفاظ على تباين عالٍ للنصوص لضمان المقروئية، لكن دون استخدام "الأبيض النقي على الأسود النقي" لتجنب إجهاد العين (Halation effect).


2. **قيد التسلسل الهرمي (Hierarchy Constraint):**
* يجب أن تكون أدوات الذكاء الاصطناعي (Correct, Improve, Summarize) مميزة بصرياً عن أدوات التنسيق التقليدية (Bold, Italic). الأولى توضع في شريط عائم أو مميز، والثانية في شريط ثابت أقل بروزاً.


3. **قيد الاشتراكات (Tier Visuals):**
* **الباقة المجانية:** واجهة قياسية نظيفة.
* **باقة Ultra:** إضافة لمسات بصرية دقيقة جداً (مثل حدود ذهبية خافتة جداً `border-yellow-500/20` أو شارة "Ultra" صغيرة بجوار الاسم) لتعزيز شعور التميز دون ابتذال.


4. **التجاوب (Responsiveness):**
* الأولوية لنسخة سطح المكتب (Desktop First) نظراً لطبيعة التطبيق (تحرير نصوص احترافي)، مع ضمان تجربة قراءة سلسة على الجوال.



---

**ملاحظة للمطورين:**
عند تطبيق هذا الدليل باستخدام `Tailwind`، اعتمد على فئات `zinc` للألوان المحايدة، واستخدموا `backdrop-blur-md` في القوائم العائمة لخلق تأثير "الزجاج المعتم" (Frosted Glass) الذي يعزز العمق والفخامة.